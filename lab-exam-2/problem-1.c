#include <stdio.h>

int main()
{
    int n, k, a;

    scanf("%d %d %d", &n, &k, &a);
    // int person = 2;
    int a = 1;
    int b = 2;
    int c = 3;
    int count = b;

    do {
        k--;
        count ;
    } while(k != 0);

    printf("%d");

    return 0;
}