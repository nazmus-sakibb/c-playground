#include<stdio.h>

int main() {
    char ch;

    scanf("%c", &ch);
    printf("%c", ch);

     for (int i = 0; i < n; i++) {
         char x = ch % 10;
        // int D = ch %10;

         if(x = 65) {
            printf("%c Anton\n", x);
         }
         else if(x = 68) {
             printf("%c Danik\n", x);
         }
    }

    return 0;
}